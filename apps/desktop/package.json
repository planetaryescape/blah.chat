{
  "name": "@blah-chat/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev --config src-tauri/tauri.conf.json",
    "build": "tauri build --no-bundle --config src-tauri/tauri.conf.json",
    "build:bundle": "tauri build --config src-tauri/tauri.conf.json",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.1"
  }
}
