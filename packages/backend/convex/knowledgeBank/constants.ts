/**
 * Knowledge Bank Constants
 *
 * Shared configuration for knowledge bank processing and search.
 */

// Embedding model - must match across processing and search
export const EMBEDDING_MODEL = "text-embedding-3-small";

// Batch size for embedding generation
export const EMBEDDING_BATCH_SIZE = 100;
