# Half-finished features audit

Scope: half-finished features only. Read `AGENTS.md` and `CLAUDE.md`; used `rg`, `git ls-files`, targeted file reads, and caller/callee tracing. No source changes made.

## Findings

| id | priority | issue | evidence file:line | impact | recommended action | confidence |
| --- | --- | --- | --- | --- | --- | --- |
| HF-001 | P1 | Scheduled memory maintenance enqueues extraction through a Trigger.dev REST path that is not present in the repo, while the live app API uses `/api/v1/memories/extract` and triggers task id `extract-memories`. | `packages/jobs/src/trigger/extract-inactive-conversations.ts:31`, `apps/web/src/app/api/v1/memories/extract/route.ts:78`, `packages/jobs/src/trigger/extract-memories.ts:399` | Automatic memory extraction for inactive conversations likely never runs, so memories depend on manual/user-triggered flows. | Replace the stale `/api/v1/tasks/extract-memories/trigger` call with the current Trigger client/task id or the current internal API contract; add an integration test that exercises `runMemoryMaintenance` with the default enqueue path. | High |
| HF-002 | P1 | BYOD setup UI/backend/schema exists, but POST setup is intentionally refused until an env gate ships with missing chat-table routing; public copy still says coming soon. | `apps/web/src/components/settings/BYODSettings.tsx:40`, `apps/web/src/app/api/v1/byod/route.ts:29`, `apps/web/src/app/api/v1/byod/route.ts:33`, `apps/web/src/app/LandingPageClient.tsx:528` | If the PostHog BYOD flag is enabled, users can open a database setup UI that cannot enable BYOD. If enabled by env, routing risk remains called out as not wired through generation. | Keep BYOD fully hidden until routing is end-to-end, or complete per-user chat DB routing and remove the POST refusal/copy mismatch. | High |
| HF-003 | P1 | Admin feature flags are accepted by the API schema but not surfaced in the admin UI and not merged on save; built-in tool toggles like `imageGeneration` and `codeExecution` are effectively write-only/drop-on-save. | `apps/web/src/lib/api/dal/adminSettings.ts:29`, `apps/web/src/lib/api/dal/adminSettings.ts:34`, `apps/web/src/lib/persistence/adminSettings.ts:51`, `apps/web/src/components/settings/admin/FeaturesSettings.tsx:62` | Operators may believe feature governance exists, but these flags cannot be managed through UI and any direct PATCH for `features` is discarded by merge logic. | Add `features: { ...current.features, ...(patch.features ?? {}) }` to the merge path and either expose UI controls or remove schema fields until they are enforced. | High |
| HF-004 | P2 | Built-in tool UX/renderers and onboarding copy exist, but generation only passes Composio tools to the model; code execution also has a standalone route with no generation tool registration. | `apps/web/src/components/onboarding/OnboardingTour.tsx:132`, `apps/web/src/components/chat/toolRenderers/index.ts:41`, `apps/web/src/components/chat/toolRenderers/index.ts:50`, `apps/web/src/lib/generation-v2/service.ts:442`, `apps/web/src/app/api/code-execution/route.ts:93` | Users are told they can ask for built-in Python/JS/web-search tools, and UI can render those tool calls, but the generation path has no built-in tool definitions to invoke them. | Register the built-in tools in `generation-v2` behind real settings/availability checks, or remove the onboarding/tool-renderer claims until execution is wired. | High |
| HF-005 | P2 | Convex blob migration uploads knowledge/feedback blobs but never updates the corresponding Postgres keys; transformers leave placeholders and the runner only updates attachments/TTS. | `packages/migration/src/transform/knowledge.ts:50`, `packages/migration/src/transform/feedback.ts:53`, `packages/migration/src/run-blobs.ts:182`, `packages/migration/src/run-blobs.ts:203`, `packages/migration/src/run-blobs.ts:277` | Historical knowledge source files and feedback screenshots can remain pointed at `migration/...` placeholders after migration even when R2 upload succeeds. | Look up PG ids through the id map or placeholder key, then update `knowledge_sources.storage_key` and `feedback_entries.screenshot_key`; fail/report if a target row is missing. | High |
| HF-006 | P2 | Image generation backend/SDK/component exists, but the component has no rendered caller; inside it, model selection is inert because `QuickModelSwitcher` is controlled with `open={false}`, no trigger, and a no-op `onOpenChange`. | `apps/web/src/app/api/v1/actions/images/generate/route.ts:19`, `packages/api-client/src/client.ts:676`, `apps/web/src/components/chat/ImageGenerateButton.tsx:81`, `apps/web/src/components/chat/ImageGenerateButton.tsx:150`, `apps/web/src/components/chat/QuickModelSwitcher.tsx:282` | The image generation action is mostly inaccessible from the product UI, and if mounted later its model selector would not open. | Add a real entrypoint or remove the dead component/API from product surface; if keeping, manage model switcher open state and pass `showTrigger`. | High |
| HF-007 | P3 | Canvas fuzzy conflict recovery is explicitly stubbed and appears unused by the current document API path. | `packages/shared/src/canvas/diff.ts:381`, `packages/shared/src/canvas/diff.ts:496`, `packages/shared/src/canvas/diff.ts:500`, `apps/web/src/lib/api/dal/canvas.ts:28` | Future/AI diff application may fall back to manual conflict handling instead of resolving moved content; current DAL uses whole-content updates, so live blast radius is unclear. | Either finish fuzzy conflict resolution and wire it to AI document edits, or remove/de-scope the unused path to avoid false confidence. | Medium |

## Skipped / uncertain

- Did not run build/lint/tests; audit-only task and no source changes.
- Did not enumerate every generated OpenAPI route/caller pair; focused on concrete half-implementation signals and traced likely live paths.
- Excluded broad docs TODOs unless source code confirmed a matching unfinished path.
- Did not inspect secrets; no secret values reproduced.
