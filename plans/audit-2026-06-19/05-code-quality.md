# Code Quality Audit 05 - 2026-06-19

Scope: code quality and improvement opportunities only. This pass focused on repo conventions called out in `AGENTS.md`: API envelopes, centralized prompts/models, cost tracking, resilient generation, Pino logging, TypeScript strictness, and use of established libraries/patterns.

## Methodology

- Read `AGENTS.md`, `README.md`, root package/config files, CI/test shape, and the `improve` audit playbook.
- Read project pattern notes for API envelopes, model configuration, prompt management, budget/cost tracking, and resilient generation.
- Used `git ls-files`, `rg`, `git log --name-only`, `wc -l`, and targeted `nl -ba` reads. No source code was mutated.
- Vetted each finding by opening cited files directly.

## Findings

| id | priority | issue | evidence file:line | impact | recommended action | confidence |
|---|---|---|---|---|---|---|
| CQ-01 | P1 | Operational LLM calls have incomplete and duplicated cost tracking. `modelPreviewDAL` says previews get a standard usage record, but only returns `result.usage`; ack and compaction calls do not persist usage either. Other operational paths hand-roll the same `usageRecords` insert. | `apps/web/src/lib/api/dal/modelPreview.ts:15`, `apps/web/src/lib/api/dal/modelPreview.ts:27`, `apps/web/src/lib/conversations/ackGeneration.ts:33`, `apps/web/src/lib/conversations/compaction.ts:31`, `apps/web/src/lib/conversations/compaction.ts:84`, `apps/web/src/lib/api/dal/summarize.ts:40`, `apps/web/src/lib/api/dal/meetingExtraction.ts:70` | Cost dashboards and budget controls can undercount model preview, ack, and compaction spend; duplicated tracking code makes future token-field changes easy to miss. | Create one shared `recordOperationalUsage` helper using `normalizeUsageTokens`/`calculateCost`, require it for every `generateText`/`generateObject`/streaming call, pass needed user/conversation context into ack/compaction/model-preview, and add PGlite tests that assert `usage_records` rows. | High |
| CQ-02 | P2 | API envelope convention drifts on pre-SSE and not-found error paths. Several v1 handlers use plain text `new Response(...)` while normal routes use `formatErrorEntity`. | `apps/web/src/app/api/v1/messages/stream/[conversationId]/route.ts:31`, `apps/web/src/app/api/v1/messages/stream/[conversationId]/route.ts:74`, `apps/web/src/app/api/v1/cli/messages/stream/[conversationId]/route.ts:30`, `apps/web/src/app/api/v1/cli/messages/stream/[conversationId]/route.ts:79`, `apps/web/src/app/api/v1/conversations/[id]/active-generation/route.ts:25`, `apps/web/src/app/api/v1/cli/conversations/[id]/messages/route.ts:29` | REST and CLI clients cannot uniformly parse failures; mobile/api-client code has to special-case text bodies around stream setup. | For errors before an SSE stream is returned, use `NextResponse.json(formatErrorEntity(...), { status })` or throw `ApiError` through `withErrorHandling`; reserve SSE `sendError` for errors after stream headers are committed. | High |
| CQ-03 | P2 | Prompt definitions are split and duplicated between app-local and shared packages. The web title/note prompts duplicate the shared prompts, while jobs import the shared version. | `apps/web/src/lib/prompts/operational.ts:34`, `apps/web/src/lib/prompts/operational.ts:44`, `packages/shared/src/prompts/title-generation.ts:1`, `packages/shared/src/prompts/title-generation.ts:11`, `apps/web/src/lib/conversations/titleGeneration.ts:14`, `packages/jobs/src/trigger/generate-title.ts:12` | Prompt behavior can drift between web-triggered and job-triggered title/note generation; tests only assert strings exist, not parity. | Move ack/compaction prompts into shared prompt modules or import shared title/note prompts from web; add a parity test for prompt exports used by web and jobs. | High |
| CQ-04 | P2 | The code execution API is a standalone route that bypasses the common route stack: no Zod body parser, raw response envelopes, and console logging instead of Pino. | `apps/web/src/app/api/code-execution/route.ts:93`, `apps/web/src/app/api/code-execution/route.ts:100`, `apps/web/src/app/api/code-execution/route.ts:116`, `apps/web/src/app/api/code-execution/route.ts:235`, `apps/web/src/app/api/code-execution/route.ts:249` | A high-risk tool path has bespoke validation/error shape/logging, making client behavior and production diagnostics less predictable than v1 routes. | Keep the E2B dynamic import if needed, but wrap the route in the same parse/envelope/logger patterns: Zod schema, `formatEntity`/`formatErrorEntity`, `logger.error`, and focused tests for malformed bodies and sandbox failures. | High |
| CQ-05 | P2 | Export/import DTOs use broad `any` and weak runtime validation at a user-data boundary. JSON import casts parsed content to `ExportData` and only checks top-level fields before mapping nested messages as `any`. | `apps/web/src/lib/export/json.ts:7`, `apps/web/src/lib/export/json.ts:12`, `apps/web/src/lib/export/markdown.ts:7`, `apps/web/src/lib/export/chatgpt.ts:23`, `apps/web/src/lib/import/parsers/json.ts:6`, `apps/web/src/lib/import/parsers/json.ts:22` | Invalid or older export files can fail mid-import or silently drop/mangle fields; TypeScript cannot protect format migrations. | Define versioned export/import Zod schemas plus concrete DTO types derived from them; validate parsed JSON before mapping and add fixtures for current, legacy, and malformed exports. | Medium |
| CQ-06 | P3 | `generation-v2/repository.ts` is a broad persistence god module spanning request creation, message graph writes, session state, usage records, routing decisions/outcomes, stream replay, and cancellation. | `apps/web/src/lib/generation-v2/repository.ts:1`, `apps/web/src/lib/generation-v2/repository.ts:523`, `apps/web/src/lib/generation-v2/repository.ts:1426`, `apps/web/src/lib/generation-v2/repository.ts:1811`, `apps/web/src/lib/generation-v2/repository.ts:2166`, `apps/web/src/lib/generation-v2/repository.ts:2222` | Resilient generation changes have high review and regression surface; unrelated state-machine, usage, and routing edits land in one 2.3k-line file. | Do not big-bang refactor. When next touching this area, extract along stable boundaries: request/session repository, message graph repository, usage repository, routing repository, replay/status repository. Preserve existing tests and add characterization tests before each extraction. | High |
| CQ-07 | P3 | Bible reference conversion mixes a battle-tested parser with custom regex/book-map conversions in multiple places. | `apps/web/src/lib/bible/parser.ts:1`, `apps/web/src/lib/bible/utils.ts:1`, `apps/web/src/lib/bible/utils.ts:60`, `apps/web/src/app/api/v1/bible/verse/route.ts:103`, `apps/web/src/app/api/v1/bible/verse/route.ts:140` | Edge cases for OSIS ranges/book aliases can diverge between link rendering, display URLs, and the verse API; fixes require touching duplicate maps/regexes. | Centralize OSIS display/query conversion in one tested module, reuse parser-library output where possible, and cover ranges/numeric books/apocrypha decisions explicitly. | Medium |
| CQ-08 | P3 | Structured logging is inconsistent in operational DAL/jobs; some paths still use `console.*` where Pino is available. | `apps/web/src/lib/logger.ts:1`, `apps/web/src/lib/api/dal/summarize.ts:58`, `apps/web/src/lib/api/dal/meetingExtraction.ts:88`, `packages/jobs/src/trigger/generate-title.ts:156`, `packages/jobs/src/trigger/extract-memories.ts:183`, `packages/jobs/src/trigger/generate-image.ts:246` | Production logs lose consistent structure, request/user context, and filtering across exactly the background/LLM paths that need cost and failure observability. | Standardize an app/jobs logger wrapper and replace `console.*` in touched operational paths with structured events carrying non-sensitive ids, model id, feature, and operation. | Medium |

## Skipped / uncertain areas

- Did not run tests, lint, build, or dependency audit; this was a read-only report pass.
- Did not deeply audit mobile UI, generated API client internals, or all 162 v1 route files.
- Did not inspect local `.env` files or reproduce any secret values.
- Some P3 items are refactor opportunities only; fix when already touching those areas, not as standalone churn.
